import { Text } from 'react-native';
import Layout from '@/layout/Layout';

export default function AtlasList() {
  return (
    <Layout statusBarStyle="dark">
      <Text className="text-foreground">Atlas (próximamente)</Text>
    </Layout>
  );
}
