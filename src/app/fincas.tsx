import { Text } from 'react-native';
import Layout from '@/layout/Layout';

export default function Fincas() {
  return (
    <Layout>
      <Text className="text-lg font-semibold text-foreground">Fincas (próximamente)</Text>
    </Layout>
  );
}
